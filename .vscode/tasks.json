{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "arduino-cli upload --input-dir build -b arduino:avr:uno -p /dev/tty.usbmodemF412FA9B42402",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}